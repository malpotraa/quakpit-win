# Quakpit for Windows — runtime + build dependencies
PySide6>=6.6,<7
requests>=2.31
google-auth>=2.28
google-auth-oauthlib>=1.2
keyring>=24.0

# Build-time only (used by build.py / PyInstaller). Safe to install everywhere.
pyinstaller>=6.3
