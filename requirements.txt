# Quakpit for Windows — runtime + build dependencies
# Google OAuth + Calendar, the credential vault, and all HTTP are stdlib now
# (urllib + http.server + ctypes), so the only runtime dependency is the GUI.
PySide6>=6.6,<7

# Build-time only (used by build.py / PyInstaller). Safe to install everywhere.
pyinstaller>=6.3
